import mongoose from "mongoose";

const resourceMaterialSchema = new mongoose.Schema(
  {
// enums of manuscrits, smitis orr anythin spitirual ReceiptPoundSterlingIcon, trian ersnal ai on this osts and resoices and forum posts     // 
  },
  { timestamps: true }
);
 
export default mongoose.model("ResourceMaterial", resourceMaterialSchema);
